#
# Streamlit Cloud runtime dependencies - minimal set.
#
# Only the packages the Streamlit demo (streamlit_app.py) needs at runtime.
# Training scripts, notebooks, and dev tooling use requirements-dev.txt.
#
# Streamlit Community Cloud currently provisions Python 3.14, so torch is
# pinned to >=2.9 (first torch version with Python 3.14 wheels on PyPI).
#

# Core ML runtime
torch>=2.9.0,<2.12
torchvision>=0.24.0,<0.27
segmentation-models-pytorch>=0.4,<0.7

# Numerics
numpy>=1.26.0

# Image pipeline (headless variant has no GUI deps - required for Cloud)
opencv-python-headless>=4.9.0
Pillow>=10.0.0,<12

# Streamlit
streamlit>=1.30.0,<2.0
